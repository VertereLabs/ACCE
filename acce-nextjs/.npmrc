# better-auth declares an optional @sveltejs/kit peer that pulls Vite 8, which
# conflicts with vitest's Vite. We don't use Svelte, so relax strict peer
# resolution to keep installs green.
legacy-peer-deps=true
