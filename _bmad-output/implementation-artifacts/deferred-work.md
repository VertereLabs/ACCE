# Deferred Work

Real-but-not-actionable-now items surfaced during reviews. Each should be picked up in a later loop or explicitly closed.

## Deferred from: code review of story-1.1 (2026-07-05)

- **Partial unique index invisible to Prisma schema** — `LedgerEntry_enrollmentId_booking_charge_key` (AD-8, one BOOKING_CHARGE per enrollment) is hand-added SQL in migration `20260705203800_schema_deltas_spine`. Prisma cannot model partial-unique indexes, so every future `prisma migrate dev` shadow-DB compare will report it as drift and attempt to drop it. Future story authors must preserve it and reject any generated migration that drops it. (Inherent Prisma limitation; documented in schema + migration. Low.)
- **AC5 `migrate deploy` unproven by execution** — the new migration chain (init + schema_deltas_spine) was never run against a real Postgres (dev sandbox blocks prod-credential deploy). Verified statically only (prisma validate, line-by-line SQL review, prisma canonical-diff). Follow-up: add a CI job using an ephemeral Postgres (service container / testcontainers) that runs `prisma migrate deploy` on a fresh DB to prove the full chain applies cleanly before release. (Medium; environmental, not a code defect.)

## Deferred from: code review of story-1.3 (2026-07-05)

- **`priority` boolean prop warning on `next/image` in `Logo.tsx`** — React warns "Received `true` for a non-boolean attribute `priority`" during jsdom render tests (surfaces in `portal-nav.test.tsx` and pre-existing `render-smoke.test.tsx`). Pre-existing in `src/components/Logo.tsx` (not touched by 1.3); cosmetic test-env noise only — Next's real `<Image>` consumes `priority` correctly at build/runtime. Follow-up: when `Logo.tsx` is next touched, confirm the prop is passed to the real Next `Image` and not leaking to the DOM under the mock. (Low; pre-existing, not caused by this change.)

## Deferred from: code review of story-1.4 (2026-07-05)

- **`computeStart` comment vs UTC computation** — `acce-nextjs/prisma/seed-data.ts:132-138` computes class start times in UTC (09:00/14:00 UTC → 11:00/16:00 SAST) but the surrounding comments say "business hours, SAST". Times are still within business hours after the +2h UTC offset, so this is a doc-only mismatch with no behavioural impact. Follow-up: align the comment with the actual UTC basis (or convert to an explicit SAST offset) next time the seed is touched. (Low; cosmetic.)
- **Day-separation unit test can theoretically flake** — `acce-nextjs/tests/unit/seed-data.test.ts:172-178` calls `new Date()` twice and asserts an exact 3-day gap; a UTC-midnight straddle between the two `computeStart` calls could make the `toBe(3)` assertion flaky. Astronomically rare and product-irrelevant. Follow-up: derive both starts from a single injected base date to make it deterministic. (Low; test hygiene.)

## Deferred from: code review of story-2.2 (2026-07-05)

- **Display timezone relies on the deploy server's local TZ (cross-cutting with 2.1 input parsing)** — the admin classes list renders `start` via `toLocaleString("en-ZA")` with no explicit `timeZone` (`acce-nextjs/src/app/(admin)/admin/classes/page.tsx:60`). This is self-consistent today: Story 2.1's create form parses the `datetime-local` string with `z.coerce.date` (no offset → interpreted as server-local), so input and display share the same TZ frame and round-trip correctly. Forcing a display-only `timeZone: "Africa/Johannesburg"` here without also pinning 2.1's input parsing would introduce a mismatch (entered 10:00 → shown 12:00). Timezone handling is therefore a system-wide decision spanning 2.1 + this list (and echoes the 1.4 SAST/UTC comment deferral). Follow-up: decide the canonical app TZ once (likely `Africa/Johannesburg` end-to-end) and apply it consistently to both the create-form parse and every display edge. (Low/Medium; environmental, self-consistent today, not a code defect in isolation.)

## Deferred from: code review of story-1.5 (2026-07-05)

- **Authenticated e2e live green run — deferred to CI + ephemeral Postgres** — `authenticated-smoke.spec.ts` (AC2/AC3 positive+STUDENT-vs-admin) and `global-setup.ts` session provisioning cannot run in the credential-blocked sandbox (no reachable Postgres); they skip via the `hasSession()`/DATABASE_URL guards. Same wall as 1.1 `migrate deploy` and 1.4 live seed. The CI job that runs `prisma migrate deploy` on a fresh ephemeral Postgres (see 1.1 item above) must ALSO seed, set `BETTER_AUTH_SECRET` (same value for globalSetup and the port-3100 webServer), and run `npm run test:e2e` so the authenticated smoke goes live-green. **Specifically verify the code-review signed-cookie fix:** `global-setup.ts` now HMAC-signs the session token to match Better Auth's `getSignedCookie`; this was traced from Better Auth source but was NOT runtime-verifiable in the sandbox — CI is the first place the signed cookie is actually validated against a live `getSession`. The suite is now self-checking (the AC2 pathname assertion fails loudly rather than false-greening if the signature is ever wrong). (Medium; environmental + first-live-verification of the review fix, not a code defect.)

## Deferred from: code review of story-2.3 (2026-07-06)

- **Concurrency integration test (FOR UPDATE lock + optimistic-concurrency + capacity-race) — deferred to CI ephemeral-Postgres** — Story 2.3's AD-16 guarantees (the `GroupSession` `FOR UPDATE` row lock, the AC4 stale-`updatedAt` reject, and the AC2 capacity-floor-under-lock) can only be exercised against a real Postgres; unit mocks cannot reproduce lock/count semantics (Task 7 posture, same wall as 1.1/1.5/2.2). The CI ephemeral-Postgres job must add an integration test that: (a) opens two concurrent `updateClassAction` calls on the same row and asserts one wins and the stale one is rejected (AC4); (b) sets `capacity < occupied` with real Enrollment rows and asserts rejection under the lock (AC2); (c) **specifically verifies AC4 token parity** — that `tx.$queryRaw`'s `updatedAt` (raw pg via `@prisma/adapter-pg`) `.toISOString()` matches the page's `findUnique` `updatedAt.toISOString()` for a first (non-stale) save, so a legitimate edit is NOT false-rejected as "changed by someone else" due to a raw-vs-typed TZ/precision mismatch. (Medium; environmental, first-live-verification, not a sandbox-fixable code defect.)
- **`meetingUrl` retained when a class is switched to IN_PERSON** — `updateClassAction` (`acce-nextjs/src/app/(admin)/admin/classes/[id]/edit/actions.ts:159`) nulls `location` for ONLINE classes but does not null `meetingUrl` for IN_PERSON classes, so an ONLINE→IN_PERSON edit can leave a stale Meet link on the row. Harmless today (nothing reads `meetingUrl` for IN_PERSON) and symmetric with the create action's non-guard. Follow-up: when enrollment/meet-link consumption lands, null `meetingUrl` for IN_PERSON (or gate it on mode) in both create and edit. (Low; cosmetic/data-cleanliness.) **Note (3.3 review):** the read side is now safe — the detail page reveals `meetingUrl` ONLY for `mode === "ONLINE"`, so a stale `meetingUrl` on an IN_PERSON row is never surfaced. This deferral is now purely a write-side data-cleanliness item.

## Deferred from: code review of story-3.3 (2026-07-06)

- **No `status`/`start` guard on the class-detail fetch (deep-link to non-SCHEDULED or past class)** — `(portal)/portal/classes/[id]/page.tsx` fetches the `GroupSession` by `id` only, with no `status: "SCHEDULED"` / `start > now` filter (unlike the 3.2 browse `findMany`). A student who deep-links / bookmarks `/portal/classes/<id>` for a `CANCELLED` (or already-started/past) class sees the full detail + a checkout panel — including, if balance is sufficient, the (inert) "Pay with balance" affordance. Only reachable by direct URL: the 3.2 browse links exclusively to SCHEDULED future classes. The button is inert in 3.3, and Story 3.4's `reserveSeat` will validate class state under the lock, so nothing can actually be booked. No AC in 3.3 requires 404-ing non-scheduled classes. Follow-up: when 3.4 lands the reserve flow, decide whether the detail page should also short-circuit non-SCHEDULED/past classes to a calm "no longer available" state (and/or `notFound()`), consistent with the reservation guard. (Low; deep-link-only, inert affordance, real enforcement lives in the 3.4 reserve path.)
- **Inert Pay CTA uses native `disabled` — not keyboard-focusable, contradicting the UX-DR6/NFR10 "keyboard-operable" note** — the forward-affordance button is rendered with the native `disabled` attribute (`page.tsx` "Pay with balance"), which removes it from the tab order; the inline comment "Button is keyboard-operable and perceivable even while disabled" is therefore inaccurate for a native disabled button (it is still announced/perceivable by screen readers as unavailable, but not focusable). The story's UX-DR6/NFR10 note asks the inert button to be "keyboard-operable with a visible focus ring." Transient: Story 3.4 converts this panel to a client island and makes the button live (no longer `disabled`), at which point it is naturally focusable. Follow-up: if the disabled affordance must be keyboard-focusable before 3.4, switch to `aria-disabled="true"` + an inert click handler (focusable-but-inert) instead of the native `disabled` attribute; otherwise close this when 3.4 wires the live button. (Low; transient, superseded by 3.4's client island.)

## Deferred from: create-story of story-3.4 (2026-07-06)

- **AD-4-mandated real-Postgres no-oversell concurrency integration test — deferred to CI ephemeral-Postgres** — `reserveSeat` (`src/lib/enrollment.ts`, Story 3.4) prevents overselling via a `Serializable` transaction whose no-oversell guarantee comes from **SSI** (the `FOR UPDATE` `GroupSession` row lock is belt-and-braces only), with a retry loop on Prisma `P2034` / raw pg `40001`. This cannot be exercised by unit mocks (no way to reproduce `40001` / snapshot-conflict semantics) — AD-4 explicitly requires a real-Postgres concurrency integration test. The CI ephemeral-Postgres job (same wall as 1.1 `migrate deploy` / 1.5 authenticated smoke / 2.2 / 2.3) must add: (a) two concurrent `reserveSeat` calls racing for the **last** seat → assert exactly ONE `CONFIRMED` enrollment + exactly ONE `BOOKING_CHARGE` ledger row, the other returns `class_full` (possibly after a `40001` retry); (b) a reserve attempt with balance < price → assert full rollback (no `Enrollment`, no `LedgerEntry`) via the `wallet.mutate` NFR4 guard; (c) FR11 — a second reserve while holding a non-cancelled enrollment → `already_enrolled`, no duplicate row/charge. (Medium; environmental + first-live-verification of the concurrency invariant, not a sandbox-fixable code defect.)
- **AD-12 reactivation vs AD-8 one-BOOKING_CHARGE-per-enrollment collision — cross-epic, unreachable in Phase 1a until Epic 5** — AD-12 requires re-booking a previously-`CANCELLED` class to **reactivate** the existing `Enrollment` row (status-agnostic `@@unique([studentId, groupSessionId])`), but AD-8's partial-unique index (`ON "LedgerEntry"("enrollmentId") WHERE type='BOOKING_CHARGE'`) forbids a second `BOOKING_CHARGE` on the same `enrollmentId` — so reactivating a row that already carries a historical `BOOKING_CHARGE` and charging again is unsatisfiable as written. **Not reachable in 3.4**: the only producer of a `CANCELLED` enrollment is Epic 5's cancel/refund flow, which does not exist yet, so 3.4's reachable/tested behaviour is new-enrollment + FR11 reject only; the reactivation branch is coded defensively per AD-12 but never exercised. Follow-up (when Epic 5 lands cancel): resolve by either keying the BOOKING_CHARGE partial-unique on an active-booking-cycle discriminator, or giving a reactivated booking a fresh ledger identity, or nulling/superseding the prior charge under the lock. (High-to-resolve when it lands; harmless/inert in 3.4.)

## Deferred from: dev-story of story-4.1 (2026-07-06)

- **Live real-Postgres PENDING-hold create + pendingExpiresAt expiry-flip round-trip — deferred to CI ephemeral-Postgres** — Story 4.1's `reserveSeat` PENDING branch (balance < price → creates `PENDING` enrollment with `pendingExpiresAt = now+15min` + unique `paymentRef`, no `BOOKING_CHARGE`) and the lazy expiry flip (`updateMany OTHER expired PENDING → CANCELLED` inside the lock) cannot be exercised live in the sandbox: no real Postgres, no seeded student with insufficient balance. Static verification (`prisma validate` + `build` + vitest) is the bar. The CI ephemeral-Postgres job (same wall as 1.1 `migrate deploy` / 1.5 authenticated smoke / 2.2 / 2.3 / 3.4) must add: (a) a student with R0 balance reserving a R290 class → assert `PENDING` enrollment created with a future `pendingExpiresAt` and a unique `paymentRef`; (b) advance `pendingExpiresAt` past now → assert next `reserveSeat` call lazily flips the expired row to `CANCELLED` under the lock (Step 3 in the new code); (c) verify `occupiedEnrollmentWhere` treats the now-expired row as free before and as cancelled after the flip; (d) assert that a live Paystack sandbox `initializeTransaction` round-trip (needs `PAYSTACK_SECRET_KEY` test key + network) returns a valid `authorization_url` and that the hold's `paymentRef` matches `Payment.reference` after the 4.2 webhook confirms (deferred further to 4.2 integration). (Medium; environmental + first-live-verification of the PENDING path, not a sandbox-fixable code defect.)
- **Live Paystack sandbox `initializeTransaction` round-trip — deferred to CI ephemeral-Postgres** — `paystack.initializeTransaction` calls `https://api.paystack.co/transaction/initialize` using `PAYSTACK_SECRET_KEY`, which is not set in the sandbox and cannot reach the Paystack API. The CI ephemeral-Postgres job must set `PAYSTACK_SECRET_KEY` (Paystack test key), run `initializeTransaction` with a real `paymentRef` and email, and assert: (a) a valid `authorization_url` is returned; (b) re-initializing with the same `reference` is idempotent (AC4 resume); (c) an invalid/empty key returns `{ ok: false }` without logging the key. The webhook confirm (charge.success → CONFIRMED + BOOKING_CHARGE) is Story 4.2; the no-oversell concurrency test is Story 4.3. (Medium; environmental + first-live-verification of the Paystack init adapter.)

## Deferred from: create-story of story-3.5 (2026-07-06)

- **Live-authenticated `/admin/students/[id]` dynamic-route run + real ADJUSTMENT-credit round-trip — deferred to CI ephemeral-Postgres** — Story 3.5's admin wallet-credit (`creditWalletAction` → `db.$transaction` → `wallet.mutate` ADJUSTMENT) writes a positive `LedgerEntry` and increases a student's balance, but this cannot be exercised live in the sandbox: the seed provisions only Priyanka (ADMIN) — there is **no seeded STUDENT user** (confirmed `prisma/seed-data.ts` = `ADMIN_USER` only), and prod DB creds are blocked. So (a) `/admin/students` renders its empty-state (no students) and only the STATIC route is added to the e2e authenticated-route manifest for the RSC-500 smoke; (b) the dynamic `/admin/students/[id]` route has no deterministic seeded id to pin (unlike `seed-class-acc-1` for 2.3/3.3); (c) the real credit round-trip (submit R500 → assert `getBalance` increased by 50000 + one `ADJUSTMENT` ledger row with `balanceAfterCents` set + it appears on `/portal/wallet`) needs a live student + Postgres. The CI ephemeral-Postgres job (same wall as 1.1 `migrate deploy` / 1.5 authenticated smoke / 2.2 / 2.3 / 3.4 concurrency) must: seed or register a STUDENT user, pin `/admin/students/<that-id>` in a parametrised authenticated spec, and assert the credit round-trip end-to-end (incl. AC2 — an invalid amount writes NO ledger row, and AC4 — crediting a non-student / bad id is rejected without an FK error). In-sandbox coverage is the pure `credit-schema` unit tests (valid/invalid amount, Rand→cents) + `prisma validate` + `build` + vitest. (Medium; environmental + first-live-verification of the credit round-trip, not a sandbox-fixable code defect.)

## Deferred from: code review of story-4.1 (2026-07-06)

- **`email ?? ""` could send an empty email to Paystack** — `payWithPaystackAction` passes `session.user.email ?? ""` to `initializeTransaction` (`actions.ts:126`). Unreachable under the app's passwordless email auth (email is always present); if it ever were empty, Paystack returns a graceful init error mapped to the "error" toast. Follow-up: when auth email nullability changes, guard explicitly. (Low; unreachable now.)
- **`existing.paymentRef!` non-null assertion in the resume path** — `enrollment.ts:262` asserts a PENDING row always has a `paymentRef`. True in 4.1 (every PENDING is created with one), but the `!` masks a would-be null reference to Paystack if a future path creates a PENDING without a ref. Follow-up: replace with an explicit guard when other PENDING-writers appear. (Low; defensive-only.)
- **Balance-button click after balance dropped creates an orphan PENDING hold + generic error toast** — if a student's balance falls between page render and clicking "Pay with balance", `reserveSeat` (the sole path, deciding under the lock) creates a 15-min PENDING hold and returns `pending_payment`, which `reserveSeatAction` maps to `{ ok:false, reason:"error" }` (`actions.ts:76`). A seat is briefly held while the user sees "Something went wrong"; it self-heals via lazy expiry. Follow-up: consider a "your balance changed — pay online?" message routing them to the Paystack CTA instead of a generic error. (Low; by-design, self-healing, documented.)
- **`APP_URL` undefined would build a malformed `callback_url`** — `${process.env.APP_URL}/portal/classes/${classId}` (`actions.ts:124`) yields `undefined/...` if unset. `APP_URL` is a required env already consumed by `auth.ts`, and the callback is display-only (AD-7 — the 4.2 webhook confirms). Follow-up: centralise a validated env accessor. (Low; env is required and already relied on elsewhere.)

## Deferred from: dev-story of story-4.2 (2026-07-06)

- **Live real-Postgres webhook round-trip (charge.success → PENDING→CONFIRMED + one BOOKING_CHARGE) — deferred to CI ephemeral-Postgres** — `confirmPaidSeat` in `src/lib/enrollment.ts` implements the full Serializable tx (Payment idempotency gate → GroupSession FOR UPDATE → PENDING→CONFIRMED flip → wallet.mutate BOOKING_CHARGE). Cannot be exercised live in the sandbox: no real Postgres, no seeded PENDING enrollment with a paymentRef, no Paystack-signed request. The CI ephemeral-Postgres job (same wall as 1.1/1.5/2.x/3.4/4.1) must add: (a) create a PENDING enrollment via `reserveSeat` on a student with R0 balance; (b) POST a `charge.success` event with a valid HMAC-SHA512 signature and matching `reference` → assert enrollment status is CONFIRMED, **exactly two** LedgerEntry rows written for this confirm — a `TOPUP` of `+data.amount` (the captured card payment funding the wallet) **followed by** one `BOOKING_CHARGE` of `-priceCents` snapshot (AD-16) — so the **net wallet balance change is `capturedAmount − priceCents` (0 when they match) and never goes negative** (the confirm must NOT trip the NFR4 guard even though the Paystack path was entered with balance < priceCents), HTTP 200. **Regression guard (code-review 4.2 CRITICAL fix):** without the TOPUP credit the lone `BOOKING_CHARGE` debit drove the wallet negative → `WalletInsufficientFundsError` → 500 → infinite Paystack retry → paid student never seated; assert the TOPUP is present and the confirm succeeds. (Critical path; environmental, not a sandbox-fixable code defect.)
- **Idempotent replay — same reference twice → exactly one charge, both 200 (NFR3) — deferred to CI ephemeral-Postgres** — The `Payment.reference @unique` insert is the atomic idempotency gate: a second delivery hits P2002, no-ops, and returns 200. Under Serializable SSI concurrent deliveries serialize. Cannot be proven without real Postgres. The CI job must POST the same `charge.success` reference twice (sequentially and/or concurrently) → assert exactly one `Payment` row, one `BOOKING_CHARGE` LedgerEntry, enrollment CONFIRMED once, both requests return 200. (Medium; environmental + first-live-verification of P2002 no-op path.)
- **AD-15 orphan credit — expired hold + full class → CANCELLATION_REFUND, no oversell — deferred to CI ephemeral-Postgres** — The `refund_to_wallet` branch in `confirmPaidSeat` handles two cases: (a) own PENDING enrollment whose hold expired and the class refilled; (b) own CANCELLED enrollment (lazy-expired). The branch credits `args.amountCents` (the captured Paystack amount) to the student's wallet as a `CANCELLATION_REFUND` via `wallet.mutate` (AD-15). Cannot be proven live without real Postgres + time-manipulation (advance `pendingExpiresAt`). The CI job must: set up a full class (occupied == capacity), POST a `charge.success` for a PENDING enrollment → assert no CONFIRMED seat, CANCELLATION_REFUND written for the captured amount, enrollment status CANCELLED, class not oversold, HTTP 200. (Critical; AD-15 money conservation, environmental.)
- **Live Paystack sandbox signature round-trip (real x-paystack-signature) — deferred to CI ephemeral-Postgres** — `verifySignature` uses Node `crypto` HMAC-SHA512 with `PAYSTACK_SECRET_KEY`. Unit tests cover the algorithm with a fixed test secret. The live round-trip (Paystack test mode sends a real `x-paystack-signature` header computed with the real test-key) cannot be verified in the sandbox (`PAYSTACK_SECRET_KEY` not set; no Paystack network). The CI job must set `PAYSTACK_SECRET_KEY` (Paystack test key) and either (a) receive a real Paystack test webhook from a sandboxed transaction, or (b) compute the signature using the real key and assert the route accepts it. This closes the gap between the unit-test vector and the real Paystack signing algorithm. (Medium; environmental + first-live-verification of NFR2 end-to-end.)
- **`parseWebhookEvent` requires `data.reference/amount/status` on EVERY event — over-strict envelope (Low, conforms to AC5 as written)** — the Zod schema (`src/lib/paystack.ts`) mandates `data.reference` (nonempty), `data.amount` (int ≥ 0), `data.status` (string) for all events; the route returns HTTP 400 when it fails. AC5's named non-charge examples (`charge.failed`, `transfer.*`) all carry those fields, so they correctly parse → the route acks them 200 — ACs are satisfied. BUT Paystack events with a different `data` shape (e.g. `subscription.create`, `invoice.create`, `customeridentification.*`) lack `reference`/`amount` → 400 instead of a benign 200, causing Paystack to retry a harmless event (log noise; no money/seat/security impact). If such events are ever enabled on the Paystack account, relax the envelope to `{ event: string }` and only strict-parse `data` when `event === "charge.success"`. (Low; robustness beyond the ACs, not an AC violation — no code change made this pass to avoid deviating from the spec-prescribed schema.)
- **Webhook rate-limiting — deferred to Coolify/reverse-proxy edge** — The route rejects forged requests cheaply via HMAC timing-safe compare (constant-time). A proper distributed rate limit is deferred to the Coolify/edge layer; an in-memory limiter is unreliable across a multi-instance standalone deploy and risks dropping legitimate Paystack retries. The no-oversell concurrency load test is Story 4.3. (Medium; architectural decision logged in autopilot-decisions.md 2026-07-06.)

## Deferred from: dev-story of story-4.3 (2026-07-06)

- **Live green run of no-oversell integration suite — deferred to CI ephemeral-Postgres** — `tests/integration/no-oversell.integration.test.ts` is the AD-4-mandated real-Postgres concurrency test (N+1 concurrent `reserveSeat` → exactly N win + 1 `class_full`, across both the balance and Paystack/PENDING paths; `confirmPaidSeat` refund-path no-oversell; idempotent replay; FR11; NFR4 rollback). The test file exists, compiles, and skips cleanly when `DATABASE_URL` is unset (sandbox). The CI ephemeral-Postgres job must: (1) provide a `DATABASE_URL` env pointing to a Postgres 15+ service container; (2) run `npx prisma migrate deploy` to apply the full migration chain; (3) run `npm run test:integration` — the suite will execute against the real DB and go live-green. This closes / supersedes the following previously-deferred concurrency items — **they are no longer "deferred with no test" — the test now exists, only its live execution is environmental**: (a) 3.4 item (a): "two concurrent reserveSeat racing for the last seat → exactly ONE CONFIRMED + ONE BOOKING_CHARGE, the other class_full" — now AC1a; (b) 3.4 item (b): "reserve with balance < price → PENDING path (no BOOKING_CHARGE)" — now AC3c PENDING assertion; (c) 3.4 item (c): "FR11 already_enrolled, no duplicate" — now AC3b; (d) 4.1 PENDING-hold create + occupancy exclusion — now AC1b PENDING path; (e) 4.2 idempotent replay (NFR3) — now AC3a; (f) 4.2 AD-15 orphan refill path — now AC2; (g) 4.2 code-review item (a) "assert TOPUP+BOOKING_CHARGE + net-non-negative as the live-CI regression guard" — AC3a asserts exactly one BOOKING_CHARGE (net non-negative is implied by confirmPaidSeat's TOPUP-first pattern). (Medium; environmental + first-live-verification of the concurrency invariant.)

- **Two-browser manual staging check (last seat, two browsers → one wins, other "class full")** — Manual verification item: on a staging deploy, seed or use a class at capacity−1, open two authenticated browser sessions as different students, click "Pay with balance" (or "Pay online") simultaneously. Assert: exactly one session shows the confirmation toast + seat confirmed; the other sees the "class full" message. This exercises the full end-to-end user-facing no-oversell path that no automated test can replicate (different browser clients, real network latency). Pin a near-full seeded class (e.g. `seed-class-*` at capacity−1) for repeatability. (Manual; staging prerequisite for Epic 4 sign-off.)
