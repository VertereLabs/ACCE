# generated: 2026-07-11
# last_updated: 2026-07-11 (3-1 dev-story: ready-for-dev -> review. Added GUIDE_PDF_PUBLISH_STATUS + isGuidePdfPublished to guides.ts and middleware.ts; 6 additive tests; vitest 57 pass / 3 pre-existing guide-route sitemap fails unchanged; tsc no new errors)
# last_updated: 2026-07-11 (3-1 create-story: backlog -> ready-for-dev; epic-3 flipped backlog -> in-progress, first story of Epic 3)
# project: ACCE
# project_key: SEO
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-07-11
last_updated: 2026-07-11T16:19:32Z
# 4-1 set ready-for-dev on 2026-07-11 (create-story, autopilot): FIRST story of Epic 4 (SEO Hygiene & Measurement). epic-4 flipped backlog -> in-progress. Codebase-wide EDITORIAL SCRUB (copy-only, additive NFR4), not new authoring. Scanned seo/epic-4 HEAD: 18 src files / 43 U+2014 occurrences, classified. IN SCOPE (rendered): guide index+part body copy (groups/page + groups/part-1..4,6, ifrs-15/page+part-1,5, ifrs-16/part-1,5), ifrs-16/page.tsx L30 metadata description, config/groupSessions.ts L70 mixSummary (rendered via GroupSessions.tsx L63). METADATA-TITLE DECISION (medium, logged): Story 4.1 RESOLVES the Epic 1 "sanctioned title em-dash exception" -- clears the em dash in the title/og.title/twitter.title of /cta-tutor (1.1), /tax-tutor (1.4), /pgda-tutor (1.6) to a colon variant (all <=60 chars, NFR2); NFR6 is unconditional site-wide + AC covers "all metadata", the Epic 1 exception was a deferral to this story, no test encodes the old titles. OUT OF SCOPE (leave): non-rendered code comments groupSessions.ts L8/L23, guides/page.tsx L51, Logo.tsx L13; test-infra em dashes in guides-config/sitemap/utils test files. En dashes (U+2013, e.g. 24-48h) and ellipsis U+2026 explicitly untouched (AC2). No blanket sed (3 no-space cases need comma/rephrase). GUARDRAIL (low, logged): add additive tests/unit/no-em-dash.test.ts (fs-read in-scope files, strip comments, assert no U+2014). Epic 1 spoke/hub pages + root page.tsx/layout.tsx already clean. Baseline to hold: full vitest 61 pass + new guard green, tsc no new errors, render-smoke H1/link asserts unaffected by title swap.
# 3-3 code-review 2026-07-11 (autopilot): CLEAN, all 6 ACs re-verified with FRESH adversarial reasoning (did not trust the dev step) -> done. THE RELEASE story; final build story of Epic 3. Diff reviewed 15fb19d..HEAD (dev commit 59b996d). AC1: both GUIDE_PUBLISH_STATUS maps (config/guides.ts + mirrored middleware.ts) flipped all-true and byte-match; middleware page branch (guideId in map && !map[guideId]) no longer 302s the 3 guides / 17 part pages. AC2: both GUIDE_PDF_PUBLISH_STATUS maps untouched all-false + PDF_TO_GUIDE intact -> /pdfs/*.pdf branch still redirects, Story-3.2 isGuidePdfPublished guards keep every download hidden on released pages, no PDFs in sitemap. AC3: sitemap.ts UNEDITED; isRouteIndexable filters on isGuidePublished so the flip auto-registers /guides + 3 index + 17 part routes; sitemap.test.ts now 6/6 green (the 3 guide-route fails RED since 1.8 RESOLVED with zero sitemap edit, exactly as AC3 predicted). AC4: two in-body <Link>/guides/ifrs-15 + /guides/ifrs-16 added to accounting-tutor related-links card (text-accent hover:underline, reuse-only); it.todo removed + AccountingTutorPage edges extended; both targets now page-live so NFR7 single-shot holds (no dead/gated internal link at release); bodyEdgeCount (nav/footer-excluded) assertion passes. AC5: exactly the superseded all-hidden asserts updated (guides-config "publishes the three reviewed guides"; render-smoke Resources 3 Available/0 Coming Soon + deep-links); PDF-guard arm (render-smoke L60-85) untouched + green; full vitest 61 pass / 0 fail / 0 todo. AC6: reuse-only, additive, tsc no new errors outside stale .next/types, 0 em dashes added (grep across 3 non-test files). 3 probes DISMISSED as noise: (1) guides-config afterEach resets flags to false against a now-true source singleton = NOT cross-file pollution (vitest default isolate:true -> per-file module registry; sitemap/render-smoke read true source in separate registries, all green); (2) `priority` non-boolean-attribute stderr = pre-existing Navbar/Hero warning outside this diff; (3) main-branch middleware.ts backport intentionally a release-time deploy step (autopilot forbids branch switch, 3.1/3.2 precedent). No HIGH/MEDIUM findings, no patches applied. Epic 3 build stories (3.1/3.2/3.3) all done; guide pages live + indexable, all PDFs held.
# 3-3 set ready-for-dev on 2026-07-11 (create-story, autopilot): THIRD/final build story of Epic 3 (already in-progress, no epic flip). THE RELEASE story. Flips the three reviewed guide PAGE flags (groups/ifrs-15/ifrs-16) to true in BOTH mirrored maps -- src/config/guides.ts GUIDE_PUBLISH_STATUS AND src/middleware.ts GUIDE_PUBLISH_STATUS (Edge can't import config; both must agree or the public build still 302s the live page to /guides). PDF flags stay all-false in both files (FR9/AC2: PDFs held) so 3.2's isGuidePdfPublished guards keep every /pdfs affordance hidden on the released pages. NO sitemap.ts edit: isRouteIndexable already filters guide URLs on isGuidePublished, so the flip auto-registers the 3 guides + 17 part routes and RESOLVES the 3 long-standing sitemap.test.ts guide-route failures (red since 1.8). SCOPE DECISION 1 (medium, logged): flip in both files, PDF maps untouched, no sitemap/helper/config edit. SCOPE DECISION 2 (medium, logged): wire the ONE pending Story-2.3 matrix edge this release unblocks -- add <Link>/guides/ifrs-15 + /guides/ifrs-16 to accounting-tutor/page.tsx (existing related-links card ~L262-277, text-accent hover:underline) and fulfil the internal-link-matrix.test.tsx it.todo (extend AccountingTutorPage edges + remove todo); deferred-work.md names 3.3 as owner, NFR7 single-shot requires no held matrix edge at release. TEST CONTRACT (medium, logged): the flip changes vitest production-branch behavior (NODE_ENV=test reads maps), so update EXACTLY the assertions encoding the old all-hidden state: guides-config.test.ts "hides guides whose flag is false" (L35-39) -> now true x3; render-smoke.test.tsx Resources block (L248-266) -> 3 Available/0 Coming Soon + deep-links present; the 3 sitemap.test.ts guide-route asserts now PASS with no edit. PDF-guard tests (render-smoke PDF arm L60-85, isGuidePdfPublished blocks) stay green untouched (PDF flags false). OUT OF SCOPE / release-time (medium, logged): main-branch middleware.ts backport is NOT done here (autopilot forbids branch switch; 3.1/3.2 established do-NOT-switch-branches); recorded as a release step. Additive/data-only: only logic edit is a Boolean flip in two mirrored maps + one page cross-link + test-assertion updates. Editorial: 4-space in guides.ts/middleware.ts/accounting-tutor, 2-space in tests, zero added em dashes. Baseline to reach: full vitest suite GREEN (the 3 guide-route sitemap fails RESOLVE here) + render-smoke PDF arm unchanged + tsc no new errors.
# 3-2 code-review 2026-07-11 (autopilot): CLEAN, all 5 ACs re-verified with FRESH adversarial reasoning (did not trust the dev step) -> done. SECOND build story of Epic 3. AC1: guard {isGuidePdfPublished("id") && (...)} yields false -> renders nothing, no <a href="/pdfs/..."> in markup (render-smoke .toBeNull() prod arm passes under vitest NODE_ENV=test production branch, all flags false). AC2: preview-env dynamic-import arm asserts the SAME /pdfs/ifrs-16-leases.pdf link present (.not.toBeNull() passes). AC3: grep confirms EXACTLY 20 /pdfs/ affordances across the guides tree (3 top-level Download-Complete-Guide cards + 17 part-page header PDF links), every one wrapped, guard key correct per file (groups/ifrs-15/ifrs-16 match GUIDE_PDF_PUBLISH_STATUS + href); no /pdfs/ reference exists elsewhere (Resources.tsx/guides index clean). AC4: reuse-only (import + JSX conditional only, no palette/hue/component); 0 em dashes in ADDED lines (grep count 0; em-dash grep hits are all pre-existing body copy outside the diff); 4-space indent preserved; additive-only (no section removed/restructured). AC5: prod-arm assertion flipped .not.toBeNull()->.toBeNull() + new preview arm locks both sides. Verification: vitest 58 pass / 3 fail (SAME pre-existing sitemap.test.ts guide-route failures, Epic-3 scope, guide URLs filtered by isGuidePublished all-false in test env, documented since 1.8/3.1) / 1 todo; render-smoke 32/32 incl. both new PDF arms; tsc no errors outside stale .next/types. 3 probes DISMISSED as noise: (1) guarded child not re-indented under && ( = cosmetic, matches story's explicit wrap-in-place directive, TS/lint-clean; (2) part-page flex gap-4 holds only the "Part N of M" label when PDF held = collapses cleanly via parent justify-between, by design; (3) Button/Download imports still referenced by guarded block = no unused-import (tsc-confirmed). No HIGH/MEDIUM findings, no patches applied.
# 3-2 set ready-for-dev on 2026-07-11 (create-story, autopilot): SECOND story of Epic 3 (already in-progress, no epic flip). Consumes isGuidePdfPublished (shipped by 3.1) to gate every /pdfs/<file>.pdf download affordance so a held PDF is never offered as a dead-ending download. SCOPE DECISION (medium, logged): gates ALL 20 affordances, not just the 3 top-level "Download Complete Guide" CTAs -- also the 17 part-page header "PDF" links (groups/part-1..7, ifrs-15/part-1..5, ifrs-16/part-1..5), because each dead-ends identically once 3.3 releases a page while its PDF stays held; wrapping all now closes that hole in one story ("leave the system working end-to-end"). Each guard is {isGuidePdfPublished("<id>") && (...)} keyed to groups/ifrs-15/ifrs-16 (ids match GUIDE_PDF_PUBLISH_STATUS + PDF_TO_GUIDE). All server components; guides/page.tsx already imports isGuidePublished so importing the PDF helper is a proven pattern. TEST CONFLICT (medium, logged): render-smoke.test.tsx L60-68 asserts the ifrs-16 part-1 PDF link PRESENT, but vitest NODE_ENV="test" => isGuidePdfPublished returns false (production branch, all flags false, per 3.1) => the guarded link is now ABSENT; flip that assertion to .toBeNull() (the correct new held-PDF contract), optionally add a preview-env re-import test asserting present. OUT OF SCOPE: flipping any publish flag (3.3), any middleware/config/guides.ts/sitemap edit (3.1 done; this story only CONSUMES the helper), main-branch backport (3.2 doesn't touch middleware). Additive-only, reuse-only styling, 4-space indent, zero em dashes. Baseline to hold: full vitest suite (3 pre-existing guide-route sitemap fails, Epic-3 scope) with render-smoke passing in its new absent shape + tsc no new errors.
# 3-1 code-review 2026-07-11 (autopilot): CLEAN, all 4 ACs re-verified with FRESH adversarial reasoning (did not trust the dev step) -> done. FIRST build story of Epic 3. AC1: page-state contract (GUIDE_PUBLISH_STATUS/isGuidePublished/isDev/isGuidesPreview/anyGuidePublished) byte-identical (diff touches ONLY the doc-comment above them, zero logic change -- git-confirmed); GUIDE_PDF_PUBLISH_STATUS + isGuidePdfPublished(id) added as a fully independent map+helper mirroring isGuidePublished's dev/preview-unlock + safe-default-false; the page=true/pdf=false independence test LOCKS the core guarantee before 3.3 relies on it. AC2: middleware page branch (L52) reads GUIDE_PUBLISH_STATUS, PDF branch (L62) now reads GUIDE_PDF_PUBLISH_STATUS via PDF_TO_GUIDE -- both mirrored Edge maps all-false, matching config by hand. AC3: all 6 flags false + identical dev/preview unlock => PDF branch byte-for-byte identical behavior in EVERY env (decouple only observable once 3.3 flips a PAGE flag true while its PDF flag stays false); full vitest suite UNCHANGED from baseline (57 pass / 3 pre-existing guide-route sitemap fails, Epic-3 scope, verified same 3 / 1 todo) + tsc no errors outside .next/types. AC4: the only 2 em dashes in guides-config.test.ts sit on PRE-EXISTING lines 19/54 (the isGuidePublished describe blocks, NOT in this diff); the added isGuidePdfPublished blocks use colons; 4-space indent held in both files. Consumers (sitemap.ts/Resources.tsx/guides/page.tsx) reference only isGuidePublished (untouched) => unaffected; zero app/guides/**/page.tsx changed (git stat empty). 2 LOW observations DISMISSED as noise: (1) GUIDE_PDF_PUBLISH_STATUS hand-duplicated across guides.ts+middleware.ts with no automated sync guard = inherent pre-existing Edge-runtime constraint acknowledged in story + project-context, not introduced here (fixing = new-tooling scope creep); (2) unconditional Download-PDF CTA on guide pages still present = explicitly Story 3.2 scope, and middleware still redirects /pdfs/*.pdf so no dead download can leak. No HIGH/MEDIUM findings, no patches applied.
# 3-1 set ready-for-dev on 2026-07-11 (create-story, autopilot): FIRST story of Epic 3 (Guide Release). epic-3 flipped backlog -> in-progress. Pure gating decouple in TWO files only: src/config/guides.ts (ADD GUIDE_PDF_PUBLISH_STATUS + isGuidePdfPublished(id), mirroring isGuidePublished's dev/preview-unlock + safe-default-false; PRESERVE GUIDE_PUBLISH_STATUS/isGuidePublished byte-identical -- consumed by Resources.tsx, guides/page.tsx, sitemap.ts) and src/middleware.ts (Edge, cannot import config: ADD mirrored GUIDE_PDF_PUBLISH_STATUS const, then in the /pdfs/<file>.pdf branch swap the read from GUIDE_PUBLISH_STATUS[guideId] to GUIDE_PDF_PUBLISH_STATUS[guideId] via PDF_TO_GUIDE; the /guides/<id> page branch keeps reading GUIDE_PUBLISH_STATUS). SAFE TODAY (AC3): all 6 flags false + identical dev/preview unlock -> PDF branch is byte-for-byte identical before/after in every env; decouple only becomes observable once 3.3 flips a PAGE flag true while its PDF flag stays false. Tests (additive): extend tests/unit/guides-config.test.ts with isGuidePdfPublished coverage incl. the AC1 INDEPENDENCE case (page=true,pdf=false) + dev-branch re-import. OUT OF SCOPE: the conditional Download-PDF CTA (Story 3.2, consumes isGuidePdfPublished in guides/<id>/page.tsx L124-138), flipping any guide live (Story 3.3), and the main-branch middleware.ts backport (release-time, do NOT switch branches). Editorial: 4-space indent both files, zero em dashes. Baseline to hold: full vitest suite (3 pre-existing guide-route sitemap fails are Epic-3 scope, expected) + tsc no new errors.
# 2-3 code-review 2026-07-11 (autopilot): 1 MEDIUM finding FIXED in-review -> done. FRESH adversarial reasoning (did not trust the dev step). The primary deliverable (tests/unit/internal-link-matrix.test.tsx) DID assert every matrix edge, but its unscoped container.querySelector('a[href="/subjects"]') on the four SPOKE pages was satisfied by the always-rendered Navbar desktop link (each page renders <Navbar/> which emits href="/subjects"), not the page body -- so a dropped in-body /subjects cross-link would have stayed GREEN, the exact regression AC1 exists to catch (probed: AccountingTutorPage renders 2 /subjects anchors, only 1 in-body). Radix Qualifications dropdown (/cta-tutor,/pgda-tutor) is portal-lazy = absent from initial render, Footer emits no matrix targets, so /subjects on spokes was the sole collision. FIX (test-only, additive, AC3/NFR4 preserved): added bodyEdgeCount() helper excluding anchors inside nav/footer (a.closest("nav")||a.closest("footer")) and assert >0 per edge -> every assertion now enforces the real page-body edge, future-proof against any chrome collision. Independently re-verified the matrix IS fully wired on disk (all 4 spoke bodies carry /cta-tutor+/pgda-tutor+/subjects at L265-286; both hubs 4 spokes+sibling; /subjects+homepage 6-edge sets) so NO dead link ships. AC2 pending /accounting-tutor->IFRS-guides recorded in deferred-work.md (Story 3.3), AC4 0 em dashes in new test + added deferred-work lines (the 1 em dash in deferred-work.md is a pre-existing Story 1.1 entry, out of scope), AC5 vitest 51 pass / 3 pre-existing guide-route sitemap fails (Epic 3) / 1 todo unchanged + tsc no new errors. Epic 2 build stories (2.1/2.2/2.3) all done.
# 2-3 set ready-for-dev on 2026-07-11 (create-story, autopilot): THIRD/final build story of Epic 2 (already in-progress, no epic flip). This is a VERIFICATION-AND-GUARD story, not new authoring: the create-story audit confirmed the entire hub<->spoke internal-link matrix is ALREADY wired (Epic 1 authored each page's outbound links in place; Story 2.2 added the homepage edges). Every non-guide edge is present: each spoke -> /cta-tutor,/pgda-tutor,/subjects; each hub -> 4 spokes + sibling hub; /subjects -> 4 spokes + both hubs (rendered via SUBJECTS/QUALIFICATIONS .map() with href={x.href}, so a literal-href grep misses them -- must render to verify); homepage 4 Learn-more + 2 hub mentions (2.2, done). DELIVERABLE (medium, logged): add ONE new additive test tests/unit/internal-link-matrix.test.tsx asserting the FULL required outbound edge set for all 7 pages + homepage (the existing render-smoke asserts only ONE representative edge per page, so a dropped edge stays green today) -- makes the matrix a durable regression contract, changes ZERO page.tsx bodies (additive-only NFR4). PENDING EDGE (medium, logged): /accounting-tutor -> IFRS guides (/guides/ifrs-15,/guides/ifrs-16) is Epic-3-gated (GUIDE_PUBLISH_STATUS groups/ifrs-15/ifrs-16 all false); wiring now would link to a "Coming Soon"-redirecting page in a public build (violates NFR7), so per AC2 it is RECORDED as pending in deferred-work.md and wired by Story 3.3 on guide release. NOT this story: re-touching reviewed/done spoke/hub pages, any Services/Navbar/sitemap/config edit. Baseline to hold: vitest 43 pass / 3 pre-existing guide-route sitemap fails (Epic 3) + new matrix test passes; tsc no new errors.
# 2-2 code-review 2026-07-11 (autopilot): CLEAN, all 7 ACs re-verified with FRESH reasoning -> done. AC1 card->spoke mapping co-located per-object href keyed to title (the two index-mismatch traps route correctly: Taxation->/tax-tutor, Management Accounting->/financial-management-tutor); AC2 Qualifications block links /cta-tutor+/pgda-tutor INSIDE Services section below grid, page.tsx byte-UNCHANGED (git-confirmed); AC3 header/grid/4 cards icon+title+description preserved, purely additive; AC4 `text-accent hover:underline` matches review-passed Epic 1 spoke precedent (accounting-tutor/page.tsx L265), reuse-only tokens, no new hue/component/variant, gold accent-only; AC5 token-driven dual-mode, next/link keyboard-focusable <a> at site baseline, Learn-more on own line (mt-4 inline-flex); AC6 em-dash grep=0, arrow is `&rarr;`(->) not em dash; AC7 all 6 route dirs on disk. Tests: vitest 43 pass / 3 pre-existing guide-route sitemap fails (Epic 3, /guides/* unpublished; verified "missing" list is guide-only, zero marketing/spoke routes) + 2 new homepage-links guardrails pass; tsc no non-.next/types errors. 3 probes DISMISSED as noise: plain-link focus-ring = pre-existing site convention (adjudicated 2.1); text-accent vs nominal accent-ink = pre-adjudicated cosmetic, precedent wins; inline Qualifications hub links = AC2 explicitly permits inline links. No HIGH/MEDIUM findings, no patches applied.
# 2-2 set ready-for-dev on 2026-07-11 (create-story, autopilot): SECOND story of Epic 2 (already in-progress, no epic flip). Wires the CAP-9 homepage-row internal links: adds a "Learn more" Link to each of the 4 Services cards mapped BY SUBJECT NAME (not array index -- file order is FinAcc,Tax,MgmtAcc,Auditing so index-mapping would mis-route Tax+MgmtAcc) -> FinAcc:/accounting-tutor, Tax:/tax-tutor, MgmtAcc:/financial-management-tutor, Auditing:/auditing-tutor; plus a short Qualifications mention linking /cta-tutor + /pgda-tutor. DESIGN DECISIONS (logged): (medium) Qualifications mention placed INSIDE the existing Services section below the grid (NOT a new page.tsx section) to satisfy AC2 without adding/restructuring a homepage section (NFR4) -> single-file change src/components/Services.tsx; (low) use next/link <Link> styled `text-accent hover:underline` matching the DONE Epic 1 spoke cross-link precedent (accounting-tutor/page.tsx ~L265). Learn-more affordance uses "->"/arrow glyph, never an em dash (NFR6). All 6 targets on disk from Epic 1 (no dead links, NFR7). NOT this story: full-matrix audit + /accounting-tutor->IFRS-guide pending edge (2.3), any /app/*/page.tsx spoke edit, page.tsx, Navbar (2.1 done). Baseline to hold: vitest 41 pass / 3 pre-existing guide-route fails (Epic 3).
# 2-1 code-review 2026-07-11 (autopilot): CLEAN, all 7 ACs re-verified with fresh reasoning -> done. AC1 Subjects->/subjects; AC2 Qualifications reaches /cta-tutor+/pgda-tutor via desktop shadcn DropdownMenu (already-installed @radix-ui/react-dropdown-menu, no new dep) + mobile flat grouped links = the AC2-permitted "dropdown desktop / grouped mobile" pattern; AC3 six other nav items + WhatsApp hero CTA + ThemeToggle byte-unchanged; AC4 Radix-keyboard trigger + ADDED explicit focus ring, mobile children py-2 pl-4 close via setIsOpen(false), open/close intact; AC5 reuse-only tokens (text-muted-foreground, hover:text-accent-ink, bg-popover), no new hue, dual-mode; AC6 em-dash grep=0; AC7 additive (only Navbar.tsx+render-smoke test), all 3 targets on disk, no homepage removed. 3 adversarial probes dismissed as noise: (1) plain nav links lacking explicit focus-visible rings is the PRE-EXISTING site convention (original Navbar+globals.css have no focus classes), not regressed; (2) desktop dropdown links portal-lazy-mounted (absent from initial SSR HTML) but crawl-discoverable via sitemap.ts (both hubs registered Story 1.8) and AC2 explicitly permits a dropdown; (3) opacity-60/text-sm mobile label are utilities not palette. Navbar has no prop change -> ~20 server-page consumers unaffected. tsc clean (only stale .next/types); vitest 41 pass / 3 pre-existing guide-route failures (Epic 3) unchanged. No HIGH/MEDIUM findings, no patches applied.
# 2-1 set ready-for-dev on 2026-07-11 (create-story, autopilot): FIRST story of Epic 2 (Navigation). epic-2 flipped backlog -> in-progress. Repoints Navbar.tsx "Subjects" from /#services to /subjects and adds a "Qualifications" affordance reaching /cta-tutor + /pgda-tutor. DESIGN DECISION (medium, logged): desktop uses the already-installed shadcn DropdownMenu (@radix-ui/react-dropdown-menu, NO new dep), mobile uses flat grouped links (Qualifications sub-heading + 2 child links) to avoid nested-menu touch complexity; plain grouped links accepted as simpler fallback per AC2. Single-file change (src/components/Navbar.tsx) + optional tiny render-smoke Navbar guardrail. All 3 link targets (/subjects,/cta-tutor,/pgda-tutor) confirmed on disk from Epic 1 -> no dead links (NFR7). Preserve: 6 other nav items, WhatsApp hero CTA (single conversion CTA), ThemeToggle, mobile open/close. Reuse existing tokens only (accent-ink for gold), dual-mode, no em dashes (NFR5/NFR6). NOT this story: homepage Learn-more links (2.2) or full matrix audit (2.3).
# 1-8 code-review 2026-07-11 (autopilot): CLEAN, all 6 ACs re-verified with fresh reasoning. Pure additive 7-route append to ROUTES (no trailing slash, at top after "/"); isRouteIndexable/getPriority/BASE_URL/changeFrequency/sitemap() body byte-identical (NFR4). AC1 BASE_URL+path canonical URLs; AC3 priority 0.8 depth-1 + weekly; AC5 no trailing slash (canonicals legitimately differ); AC6 zero em dashes (grep-confirmed). AC4: the 7 new routes now register and are ABSENT from the sitemap.test "missing" list (the 21-item list is /guides/* ONLY, zero marketing routes) -- the 3 remaining failures are guide routes filtered by isRouteIndexable (unpublished in test env), byte-identical to baseline, genuinely Epic 3 scope. Full suite 39 pass / 3 pre-existing guide failures unchanged; tsc no new errors. No HIGH/MEDIUM findings (AC4 "goes GREEN" wording is an optimistic doc nuance, not a code defect). -> done. Epic 1 now sitemap-complete; all 7 new pages crawler-discoverable.
# 1-8 dev-story 2026-07-11 (autopilot): appended 7 new routes to ROUTES array in sitemap.ts (additive-only, NFR4). No logic changes. 39 unit tests pass, 3 pre-existing guide-route failures unchanged. tsc no new errors. Status: review.
# epic-1 marked in-progress and 1-1 set ready-for-dev on 2026-07-11 (create-story: /cta-tutor flagship hub contexted; sitemap/navbar/homepage links scoped to later stories; cross-links to unbuilt siblings authored in place per NFR7)
# 1-1 code-review 2026-07-11 (autopilot): clean, all 7 ACs verified, no HIGH/MEDIUM findings -> done. Sitemap registration for /cta-tutor deferred to Story 1.8 by AC7 design.
# 1-2 set ready-for-dev on 2026-07-11 (create-story, autopilot): /accounting-tutor spoke contexted off the DONE /cta-tutor template. Service+FAQPage schema; outbound links scoped to /cta-tutor,/pgda-tutor,/subjects (IFRS-guide edge deferred to 2.3/Epic 3 gate); CAP-2 meta em dash -> colon per NFR6; sitemap deferred to 1.8. NFR1 differentiation flagged as top risk.
# 1-2 code-review 2026-07-11 (autopilot): clean, all 7 ACs verified with fresh reasoning, zero em dashes, differentiation (NFR1) satisfied, no HIGH/MEDIUM findings (2 low observations dismissed) -> done. sitemap.test /accounting-tutor failure is by-design deferred to Story 1.8.
# 1-3 set ready-for-dev on 2026-07-11 (create-story, autopilot): /financial-management-tutor (MAF) spoke contexted off the DONE /accounting-tutor template. Service+FAQPage schema; outbound /cta-tutor,/pgda-tutor,/subjects (no IFRS-guide edge for this page); CAP-3 meta em dash -> colon per NFR6 (Story 1.2 precedent); sitemap deferred to 1.8. Top risks: NFR1 differentiation (MAF costing/budgeting/decision-making/finance, not IFRS) + AC3 dual-naming (target both "financial management" and "management accounting").
# 1-3 code-review 2026-07-11 (autopilot): CLEAN, all 7 ACs verified with fresh reasoning; NFR1 differentiation satisfied (MAF costing/budgeting/decision-making/corporate-finance, distinct from /accounting-tutor IFRS); AC3 dual-naming satisfied (H1 opening + "What MAF covers" H2 + first FAQ); zero em dashes; render-smoke 15/15 green; tsc no new errors; no HIGH/MEDIUM findings (2 low observations dismissed: title 57ch still <=60, word-count overrun pre-adjudicated per 1.2) -> done. The 3 sitemap.test failures (/cta-tutor,/accounting-tutor,/financial-management-tutor + guides) are by-design deferred to Story 1.8.
# 1-4 set ready-for-dev on 2026-07-11 (create-story, autopilot): /tax-tutor spoke contexted off the DONE /accounting-tutor + /financial-management-tutor template. Service+FAQPage schema; outbound /cta-tutor,/pgda-tutor,/subjects (no IFRS-guide edge for this page); CAP-4 title kept verbatim WITH its em dash = the single sanctioned exception (Story 1.1 precedent, title 49ch); CAP-4 meta em dash -> colon per NFR6 (Story 1.2/1.3 precedent, ~135ch); sitemap deferred to 1.8. Top risk: NFR1 differentiation (Income Tax Act/corporate tax/VAT/CGT/estate duty, distinct from IFRS and costing/finance).
# 1-4 code-review 2026-07-11 (autopilot): CLEAN, all 7 ACs verified with fresh reasoning; NFR1 differentiation satisfied (Income Tax Act s11(a)/individual+corporate tax/VAT Act/CGT Eighth Schedule/estate duty s4A, distinct from IFRS and costing/finance); title=49ch sanctioned em-dash exception + meta=135ch colon variant (zero em dashes anywhere but the title, grep-confirmed); Service+FAQPage JSON-LD from single FAQ_ITEMS source; three outbound links present; render-smoke 18/18 green; tsc no new errors; no HIGH/MEDIUM findings (2 dismissed: word-count overrun pre-adjudicated per 1.2/1.3, AC2-mandated OG/Twitter title mirror) -> done. The sitemap.test failures (/cta-tutor,/accounting-tutor,/financial-management-tutor,/tax-tutor + guides) are by-design deferred to Story 1.8.
# 1-5 set ready-for-dev on 2026-07-11 (create-story, autopilot): /auditing-tutor (Auditing) spoke contexted off the DONE /tax-tutor template. Service+FAQPage schema; outbound /cta-tutor,/pgda-tutor,/subjects (no guide edge for this row); title "Auditing Tutor for CA(SA) Students | ACCE Tutors" (48ch) has NO em dash so NO sanctioned exception -> WHOLE page (title+meta+body+FAQ+JSON-LD) must be em-dash-free (differs from 1.1/1.4); CAP-5 meta em dash -> colon per NFR6 (146ch); sitemap deferred to 1.8. Top risks: NFR1 differentiation (ISAs/audit process/assertions/audit-risk-model/opinions/ethics-King-IV, distinct from IFRS+costing+tax) + the story-specific "extract shared homepage-Auditing content" clause interpreted as NOT-triggered (homepage Auditing is a one-sentence Services.tsx card; do NOT edit Services.tsx - flagged medium).
# 1-5 code-review 2026-07-11 (autopilot): CLEAN, all 7 ACs re-verified with fresh reasoning; NFR1 differentiation genuinely satisfied (ISA 200/240/315/330/500/700/705/706, assertions, audit-risk-model x materiality, opinion types, SAICA/IRBA Code, King IV; "assurance OVER info others prepared" framing; distinct from IFRS+costing+tax); zero em dashes grep-confirmed (no sanctioned exception on this page); title 48ch + meta 146ch colon-variant; Service+FAQPage JSON-LD from single FAQ_ITEMS source; three outbound links (/cta-tutor,/pgda-tutor,/subjects) present; additive-only footprint (only auditing-tutor/page.tsx + render-smoke.test.tsx; no sitemap/Services/Navbar/config touched); render-smoke 21/21 green; tsc no new errors; no HIGH/MEDIUM findings (2 dismissed: word-count overrun pre-adjudicated per 1.2/1.3/1.4, 7th h2 final-CTA matches sibling template) -> done. sitemap.test failures for the new route are by-design deferred to Story 1.8.
# 1-6 set ready-for-dev on 2026-07-11 (create-story, autopilot): /pgda-tutor SECOND qualification hub contexted off the DONE /cta-tutor template (Course+FAQPage schema, NOT the spokes' Service). Links out to all 4 spokes + /cta-tutor (the OTHER hub, not self). Title "PGDA Tutor — Postgraduate Diploma in Accounting | ACCE" (54ch) = single sanctioned em-dash exception (CAP-7 verbatim, 1.1/1.4 precedent); CAP-7 meta em dash -> colon (~150ch) per NFR6; body/FAQ/JSON-LD em-dash-free. sitemap deferred to 1.8. TOP RISK: NFR1 differentiation - PGDA and CTA are the SAME qualification, so /pgda-tutor must take the QUALIFICATION framing (postgraduate diploma, entry/admission, UNISA distance angle, PGDA->CTA->CA(SA) pathway as progression) and NOT retell /cta-tutor's ITC/exam-technique story; subjects presented as link-out cards, not deep re-explanations.
# 1-6 code-review 2026-07-11 (autopilot): CLEAN, all 7 ACs re-verified with fresh reasoning; NFR1 differentiation GENUINELY satisfied (PGDA-as-qualification framing: entry/admission + UNISA distance + honours diploma-year structure; pathway H2 = progression narrative ITC->TOPP/TIPP->APC->CA(SA), distinct from /cta-tutor exam-technique; subjects as link-out cards; no shared paragraphs with /cta-tutor); title 54ch sanctioned em dash + meta 151ch colon-variant; em-dash grep = exactly 3 sanctioned title fields, zero in body/FAQ/meta (NFR6); Course+FAQPage JSON-LD as two Script blocks, provider @id correct, FAQPage.mainEntity + on-page FAQ from single FAQ_ITEMS source; all 4 spokes + /cta-tutor outbound (no self-link, route dirs exist); 3 hero CTAs across 3 view groups; additive-only footprint (only pgda-tutor/page.tsx + render-smoke.test.tsx; sitemap.ts untouched); render-smoke 24/24 green; tsc no NEW errors in touched files; no HIGH/MEDIUM findings (2 dismissed: word-count upper-bound overrun of genuine content per 1.2/1.3/1.4, OG/Twitter em-dash title mirror mandated by AC2) -> done. sitemap.test failure for the new route is by-design deferred to Story 1.8.
# 1-8 set ready-for-dev on 2026-07-11 (create-story, autopilot): FINAL epic-1 story. Registers all 7 new routes (/subjects,/accounting-tutor,/financial-management-tutor,/tax-tutor,/auditing-tutor,/cta-tutor,/pgda-tutor) in src/app/sitemap.ts ROUTES (pure additive append, NFR4; getPriority/isRouteIndexable/guide-filter untouched). Clears the 3 by-design-deferred sitemap.test.ts failures accumulated across 1.1-1.7. CRITICAL gotcha authored in-story: sitemap entries use NO trailing slash (match ROUTES convention + collectPageRoutes test form) even though page canonicals use "/route/" -- a trailing-slash entry would leave the "every page.tsx registered" assertion RED. All 7 route dirs confirmed present on disk.
# 1-7 set ready-for-dev on 2026-07-11 (create-story, autopilot): /subjects hub contexted off the DONE /cta-tutor shell template. SCHEMA IS BreadcrumbList ONLY (first Breadcrumb JSON-LD on the site; NOT Course/Service/FAQPage) - Home->Subjects 2-item trail with ABSOLUTE item URLs (schema.org requirement, differs from relative canonical) authored explicitly in-story since no in-repo precedent. Links out to all 4 spokes + BOTH hubs (/cta-tutor,/pgda-tutor), no self-link. CAP-1 title HAS an em dash but this page has NO sanctioned exception (unlike CAP-6/CAP-7 hubs) -> title+meta both colon-variant, WHOLE page em-dash-free like /auditing-tutor. Content depth set to ~450-650 words (medium decision): CAP-1 omits the 700-900 pin for this row; a hub distributes not deep-dives, so padding toward 900 by re-explaining spokes would create the exact NFR1 doorway risk - subjects are one-line link-out cards. sitemap deferred to 1.8.
project: ACCE
project_key: SEO
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: Hub-and-Spoke Ranking Pages
  epic-1: in-progress
  1-1-cta-qualification-hub-page-cta-tutor-flagship: done
  1-2-accounting-subject-spoke-accounting-tutor: done
  1-3-financial-management-maf-subject-spoke-financial-management-tutor: done
  1-4-tax-subject-spoke-tax-tutor: done
  1-5-auditing-subject-spoke-auditing-tutor: done
  1-6-pgda-qualification-hub-pgda-tutor: done
  1-7-subjects-hub-subjects: done
  1-8-register-new-routes-in-sitemap: done
  epic-1-retrospective: optional

  # Epic 2: Navigation & Internal-Link Matrix
  epic-2: in-progress
  2-1-repoint-the-navbar-to-the-new-pages: done
  2-2-add-homepage-links-into-the-new-pages: done
  2-3-complete-and-verify-the-internal-link-matrix: done
  epic-2-retrospective: optional

  # Epic 3: Guide Release (pages live, PDFs held)
  epic-3: in-progress
  3-1-decouple-guide-page-gating-from-pdf-gating: done
  3-2-conditional-download-pdf-cta-on-guide-pages: done
  3-3-release-the-three-reviewed-guide-pages-pdfs-held: done
  epic-3-retrospective: optional

  # Epic 4: SEO Hygiene & Measurement
  epic-4: in-progress
  4-1-site-wide-em-dash-cleanup: review
  4-2-submit-new-urls-for-indexing-and-set-up-monthly-measurement: backlog
  epic-4-retrospective: optional
