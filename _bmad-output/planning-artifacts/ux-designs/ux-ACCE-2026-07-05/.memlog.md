---
topic: ACCE Tutors visual redesign (fresh look) — Stage 1
updated: 2026-07-05T11:05
---

- (decision) Scope: Stage 1 = visual redesign only (the 'fresh look'). Name 'ACCE Tutors' stays. Spec-only phase — NO code changes to acce-nextjs. Deliverable: DESIGN.md + EXPERIENCE.md that a later implementation story ports into globals.css + tailwind.config.ts.
- (decision) UI system: shadcn/ui on Next.js 16 + Tailwind 3 (existing app). DESIGN.md is a brand-layer DELTA only (override primary/accent/fonts/radius; inherit the rest).
- (decision) Brand anchors fixed by the new logo (navy A on open book + mortarboard + gold swoosh): navy #002251 = hsl(215 100% 16%) -> --primary; gold #cb8a00 = hsl(41 100% 40%) -> --accent. Palette = navy + gold on white. Earlier teal/coral + indigo/emerald options dropped (fought the logo).
- (decision) Three candidate directions to evaluate (all navy+gold; vary base + type, not brand colours): A Light Authority (cool near-white base, Playfair/Fraunces + Inter, standard logo); B Deep Authority (ink-navy dark base, gold-forward, Playfair + Inter, REVERSED white logo); C Warm Scholar (warm ivory base, Fraunces + DM Sans, standard logo). Full tables in docs/BRAND-DIRECTIONS.md.
- (event) Sources absorbed: docs/BRAND-DIRECTIONS.md, docs/index.md, docs/STRATEGY.md, docs/PHASE-1A-PLAN.md. Current app: Playfair Display + DM Sans, dark-navy + burnt-orange 'A' monogram (old look). Logo assets in design-artifacts/logo/ (transparent + reversed + sizes).
- (assumption) Form-factor: responsive web (marketing site). Surfaces to evaluate the look on = homepage (11 stacked sections: Navbar/Hero/Services/About/HowItWorks/WhyChoose/Pricing/Resources/Blog/CTA/Footer) + guides library. Portal surfaces come in Stage 2, reusing the frozen tokens.
- (event) discovery intake complete from prior docs
- (event) Rendered 3 full-homepage HTML mocks (all 11 sections, real ACCE copy) from ONE shared template stamped with each direction's tokens — identical layout, only color/type/radius/logo vary. Files: .working/mock-{A-light-authority,B-deep-authority,C-warm-scholar}.html + full-page screenshots shot-*.png. Real logo used (standard for A/C, reversed for B). Google Fonts via CDN with system fallback (offline-safe).
- (event) Awaiting Luke's direction pick (A/B/C) from the rendered mocks. Once picked -> freeze DESIGN.md token table + EXPERIENCE.md, then key-screen mock of winner, then finalize.
- (decision) WINNER: Direction B 'Deep Authority' (ink-navy + gold, dark) is the DEFAULT/primary look. Direction C 'Warm Scholar' (warm ivory + navy + warm gold) becomes the LIGHT MODE variant. So ACCE ships a two-mode theme (dark default + light), both on the navy+gold anchors, using shadcn's :root / .dark token convention. App is already dark today -> defaulting to dark is the smaller change. Reversed logo for dark, standard logo for light.
- (decision) FINAL two-mode system frozen: unified type (Playfair Display + Inter) + unified radius (0.625rem) across both modes; only palette flips. Dark (Deep Authority) = default via next-themes defaultTheme=dark; Light (Warm Scholar) = alternate. DESIGN.md + EXPERIENCE.md written to status:final.
- (event) spines finalized
- (event) Reviewer Gate ran (rubric + brand-fidelity lenses). Verdicts: rubric adequate/strong except token-completeness thin + visual-ref thin; fidelity strong on tokens but values-swap thin (old burnt-orange layer). 1 critical, 7 high across both.
- (change) Applied spec fixes from review: promoted accent-ink/primary-ink/footer-bg/footer-fg to REAL per-mode colors tokens (+fixed {colors.accent-ink} refs); added tag/theme-toggle/ghost-on-primary component tokens+rows; clarified anchor hexes are provenance (mode triples authoritative); fixed Tag cross-mode description; aligned contrast wording; added responsive-reflow note; named the 2 final mocks inline in both spines; fixed src/config/guides.ts path; promoted Flow 2 (Sipho) + added guides-library flow (Nomsa) with failure paths; archived A/B/C candidate mocks.
- (decision) The CRITICAL + several HIGH fidelity findings are NOT spec defects — they are implementation-story work (purge old burnt-orange layer in globals.css + audit 42 consumer files; next/font migration DM Sans->Inter; fix next-themes enableSystem/disableTransitionOnChange; build theme-toggle + theme-aware logo; copy logo assets to public/). review-fidelity.md holds the full checklist.
- (event) Consolidated validation-report.html + .md written and opened. Spines remain status:final; all spec-level findings resolved, implementation findings deferred to port story.
- (event) IMPLEMENTED foundation + dark default + homepage into acce-nextjs on git branch 'redesign-fresh-look' (baseline commit 18c40a0, work commit 2dcd45b). globals.css both-mode token blocks + helpers + purged burnt-orange; next/font Playfair+Inter; next-themes enableSystem=false; new Logo + ThemeToggle; Navbar/Footer rethemed; 9 homepage components migrated text-primary-foreground->foreground/muted; logos in public/. Verified: 21 unit + 26 e2e green; screenshots app-dark.png/app-light.png + hero-*.png show both modes correct (reversed logo+moon on dark, standard logo+sun on light, gold gradient headline).
- (decision) REMAINING redesign work: (1) guides pages — 837 text-primary-foreground across 21 files NOT migrated (dark text on dark in dark mode; gated Coming Soon in prod but visible in dev) — next batch, same rules. (2) Light-mode polish: hero glass card bg-primary/35 + CTA bg-white/10 translucent panels read low-contrast in light; outline buttons border-white/30 near-invisible on light. (3) favicon/OG/apple-icon refresh to new logo. (4) contrast audit muted-foreground both modes.
- (event) GIT STRUCTURE (important): acce-nextjs is its OWN git repo (not part of the outer repo — outer tracks it as a gitlink). Source redesign committed IN acce-nextjs on branch 'redesign-fresh-look' commit a9146b8; UNDO point = its parent a6a9a4f (clean pre-redesign). Outer repo (project root) branch 'redesign-fresh-look' holds the _bmad-output planning artifacts + gitlink. To revert the app: cd acce-nextjs && git checkout a6a9a4f -- . (or checkout the feat/bmad-setup branch).
- (event) STAGE 1 REDESIGN COMPLETE across the whole marketing site: foundation + dark + light + homepage (11 comps) + guides (21 files) + Hero/CTA polish + icons/OG rebrand. 21 unit + 26 e2e green. Screenshots v2-*.png in .working/. Both modes verified on home + guides. Remaining nice-to-haves only: broader contrast audit, optional SVG logo. Ready for Stage 2 (portal PRD).
