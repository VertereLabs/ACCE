---
scope: ACCE Phase 1a — Group Classes MVP (portal: auth, class booking, seat payments, wallet, admin) added additively to the existing acce-nextjs marketing app
purpose: build-substrate
altitude: feature
updated: 2026-07-05T21:31
---

- (direction) Fast path, spine-only deliverable. Feature-altitude target-state architecture for Phase 1a; keeps epics coherent. Existing docs/architecture.md is a stale pre-rebrand current-state scan, NOT the target.
- (constraint) Brownfield: additive to existing acce-nextjs (Next 16 App Router, standalone, Tailwind3+shadcn, React19+compiler). Marketing routes and SEO/headers must stay untouched (NFR8). Phase 0 already partly scaffolded: prisma/schema.prisma migrated (20260705182521_init), src/lib/db.ts singleton, .env.example, providers.tsx all exist.
- (decision) AD paradigm: server-first layered app on Next App Router. Route groups (portal)/(admin) added additively alongside flat marketing routes. Layers: route/UI (RSC + client islands) -> server actions/route handlers -> src/lib domain modules -> Prisma singleton. Client components never access DB or business logic.
- (decision) AD Single data gateway [ADOPTED]: src/lib/db.ts (Prisma + @prisma/adapter-pg + pg.Pool singleton) is the ONLY DB access. Never new PrismaClient() elsewhere (NFR6). Already in code.
- (decision) AD Additive route-group isolation [ADOPTED]: (portal) requires STUDENT, (admin) requires ADMIN, guards live in the route-group LAYOUT (server-side session check), not per-page, to avoid missing guards and the RSC-500 trap. Marketing routes untouched. Portal served at portal.accetutors.co.za. Binds FR2,FR3,NFR8,NFR11.
- (decision) AD Single canonical seat reservation: ALL seat acquisition (wallet-balance path AND Paystack path) goes through one reserveSeat() in src/lib/enrollment.ts. It runs a Serializable tx that locks the GroupSession row (SELECT ... FOR UPDATE) BEFORE counting occupied vs capacity. No other code may create/confirm an enrollment. Prevents oversell and two divergent reservation impls. Binds FR7,FR8,FR10,FR11,NFR1.
- (decision) AD Capacity is derived, never stored: occupied = count(Enrollment where status in PENDING,CONFIRMED and (pendingExpiresAt is null or > now)); seatsLeft = capacity - occupied. Expired PENDING excluded and lazily flipped to CANCELLED. No denormalized seat counter anywhere. Prevents counter-vs-reality drift. Binds FR4,FR9,FR10.
- (decision) AD Wallet = append-only ledger, balance derived: balance = sum(LedgerEntry.amountCents) for a student; every mutation appends an immutable row via wallet.addEntry(tx,...) INSIDE the caller's tx, writing balanceAfterCents. Balance may never go negative. Per-student balance mutations MUST be serialized (under the same lock as reservation, or an explicit per-student lock) so concurrent charges cannot compute a stale balanceAfterCents. Binds FR13,FR15,NFR4,NFR5.
- (decision) AD Payments confirm out-of-band via idempotent webhook: the Paystack webhook (raw-body HMAC-SHA512 verified, x-paystack-signature) is the SOLE authority that flips PENDING->CONFIRMED and writes the BOOKING_CHARGE ledger row for the Paystack path. Idempotent by Payment.reference (unique upsert): re-delivery = one charge. The browser callback/redirect NEVER confirms a seat (display-only). Binds FR8,NFR2,NFR3.
- (decision) AD Exactly one BOOKING_CHARGE per confirmed enrollment: balance path writes BOOKING_CHARGE at reserve-time (immediate CONFIRMED); Paystack path writes it at webhook confirm-time only. These paths are mutually exclusive per enrollment. Prevents double-charge / missing charge. Binds FR7,FR8.
- (decision) AD Money is integer cents (ZAR): every monetary value is an integer *Cents field; no floats anywhere; format to Rand only at the UI edge. Refund tiers computed in cents server-side. Prevents rounding drift. Binds all money FRs.
- (decision) AD Join-details gated by CONFIRMED: GroupSession.meetingUrl/location revealed ONLY to a viewer holding a CONFIRMED enrollment for that class; enforced in the server data-fetch layer (never sent to client for non-confirmed viewers), not merely hidden in the component. Binds FR5,FR6.
- (decision) AD Cancellation tiers server-enforced from a single constant: refund% computed server-side from hours-to-start (>=48h 100 / 24-48h 70 (30 fee) / <24h or no-show 0), held in one CANCELLATION_TIERS constant (group variant swappable). Cancel -> CANCELLATION_REFUND (+ CANCELLATION_FEE if any) ledger rows, enrollment CANCELLED, seat returns to derived pool. Binds FR12,FR13,FR14.
- (decision) AD Re-enrollment reuses the row (ADVERSARIAL FIX): @@unique([studentId,groupSessionId]) is status-agnostic, so a student re-booking a class they previously CANCELLED must REACTIVATE/upsert the existing Enrollment row (reset status/priceCents/pendingExpiresAt), never INSERT a new one. A blind insert throws a unique violation on legitimate re-booking. reserveSeat() must handle both create-and-reactivate. Binds FR11.
- (decision) AD External I/O via native fetch behind thin adapters (NFR9): Paystack (src/lib/paystack.ts: init + signature verify) and Resend (src/lib/email.ts) use native fetch, no axios/vendor SDK. Meetings behind a MeetingProvider interface with ManualProvider for 1a (GoogleMeetProvider later, no call-site change). Email-send failure must NOT roll back a confirmed seat (log and move on).
- (decision) AD Auth ownership [ADOPTED]: Better Auth owns user/session/account/verification tables (already in schema.prisma). role String default STUDENT is the single guard key (STUDENT|ADMIN via admin plugin). Magic-link plugin = passwordless signup (default link expiry 5min). Binds FR1,FR2.
- (version) Pinned (from acce-nextjs/package.json, ratified): next 16.1.1, react/react-dom 19.2.3, typescript ^5, tailwindcss ^3.4.17, prisma/@prisma/client/@prisma/adapter-pg ^6.7.0, pg ^8.13.1, better-auth ^1.2.8, zod ^4.3.4, @tanstack/react-query ^5.90.16, react-hook-form ^7.69.0, next-themes ^0.4.6, sonner ^2.0.7, vitest ^3.2.6, @playwright/test ^1.61.1.
- (version) Web-verified 2026-07: Better Auth latest is v1.6.22 (Jun 2026) — installed floor ^1.2.8 will resolve to 1.6.x via caret but the pinned floor is stale; recommend bumping floor to current 1.6.x and pinning exact (auth = high-value supply-chain target, per lessons-learned). Paystack webhook = HMAC-SHA512 of RAW body via x-paystack-signature (plan correct); Paystack advises signature verify OR IP allowlist.
- (assumption) [ASSUMPTION] Portal deploys as the SAME standalone container/app as marketing (one Next app, route groups), just reachable via the portal subdomain — not a separate service. Coolify Docker, base dir /acce-nextjs, Postgres on Coolify. Confirm subdomain routing is a host mapping, not a second deployment.
- (assumption) [ASSUMPTION] Lazy expiry sweep (expired PENDING treated as free + flipped on next touch) is sufficient for MVP; no cron. A belt-and-braces Coolify cron is deferred (matches plan).
- (assumption) [ASSUMPTION] Webhook route uses Next route-segment config to read the RAW body (no body parsing) and is exempt from any CSRF/auth middleware; it authenticates via HMAC only. Lives at src/app/api/webhooks/paystack/route.ts.
- (question) OPEN: How does a student first acquire wallet balance in Phase 1a? No top-up UI (out of scope) and refunds only arrive after a Paystack purchase+cancel. Balance-pay path (Epic 3) may be test-only until an admin ADJUSTMENT-credit path or seeded balance exists. (Mirrors readiness report M1.) Non-blocking for architecture; flag for epics.
- (question) OPEN: Serializable isolation ret/serialization-failure handling — under SERIALIZABLE, concurrent reservations may raise 40001; need a bounded retry policy on reserveSeat(). Confirm retry-on-serialization-failure is in the reservation contract. Non-blocking, note for dev.
- (event) Code sweep + web verification complete; all load-bearing decisions logged. Proceeding to distill spine.
- (event) Reviewer gate: lint clean (0). 4 semantic lenses (versions, adversarial, rubric, data-integrity) returned. Correctness core AD-4/AD-7 verified SOUND (interactive Serializable tx, raw-body HMAC). Applying fixes.
- (decision) AD-3 REVISED (rubric HIGH): Next.js layouts are NOT a security boundary (a direct RSC request can render a page without its layout). Authz is enforced server-side in the Data Access Layer / at every server action + route handler entry (session+role check before any domain call), plus middleware for coarse redirect and the layout redirect for UX. No page/action trusts an ancestor layout for authz. Generalizes AD-10's server-side gating.
- (decision) AD-1 REVISED (rubric MED): security headers may be EXTENDED additively for the portal (e.g. connect-src/form-action gain the Paystack host if the checkout integration needs it) but existing marketing directives are never weakened or removed. Reconciles AD-1 with the Deployment section.
- (decision) AD-4 REVISED (data-integrity MED + version reviewer): the no-oversell GUARANTEE comes from SERIALIZABLE SSI, not the FOR UPDATE row lock alone (a plain count reads child rows under a snapshot FOR UPDATE does not refresh). FOR UPDATE additionally serializes concurrent writers on the same class as belt-and-braces. reserveSeat retries on BOTH Prisma P2034 and raw pg 40001 with bounded backoff. Any isolation downgrade silently reintroduces oversell -> require a REAL-Postgres concurrency integration test (unit mocks cannot exercise 40001).
- (decision) AD-5 REVISED (adversarial HIGH): read/listing paths compute occupancy treating expired PENDING as free but MUST NOT issue an UPDATE. The actual PENDING->CANCELLED expiry flip happens ONLY inside a locked mutation in enrollment.ts (under the GroupSession FOR UPDATE), never from a reader, so it cannot race the webhook confirming that same seat.
- (decision) AD-6 REVISED (all 3 semantic reviewers, HIGH/CRITICAL): mandate ONE mechanism, not an either/or. Every wallet mutation goes through a single wallet.mutate(tx, studentId, ...) helper that first takes a per-student lock pg_advisory_xact_lock(hash(studentId)) (or SELECT ... FOR UPDATE on the user row) in the SAME tx. Row-lock-on-GroupSession is struck as a balance-serialization mechanism (it does not serialize same-student ops across different classes). Closes same-student cross-class double-spend / stale balanceAfterCents.
- (decision) AD-8 REVISED (data-integrity HIGH): 'exactly one BOOKING_CHARGE' gets a DB mechanism: a partial unique index ON LedgerEntry(enrollmentId) WHERE type='BOOKING_CHARGE' (raw-SQL migration; Prisma has no native partial-unique), AND the webhook no-ops if the enrollment is already CONFIRMED. Prevents AD-12 reactivation racing the webhook into a double charge.
- (decision) AD-7 REVISED (data-integrity MED): ordering is explicit and concurrency-safe. In one tx the webhook (1) INSERTs Payment by unique reference as the idempotency GATE (a concurrent duplicate hits the unique violation and no-ops), then (2) re-checks the enrollment/seat under the GroupSession lock, then (3) flips status + writes BOOKING_CHARGE by CALLING enrollment.ts (it does not write status itself). Pin export const runtime='nodejs' on the webhook route.
- (decision) AD-11 REVISED (data-integrity MED): pin tier comparators to remove boundary ambiguity: hoursToStart>=48 ->100%, 24<=h<48 ->70%, h<24 or no-show ->0%. Refund/fee is a SINGLE decomposition: refundCents computed from tier, feeCents = priceCents - refundCents (never computed independently) to avoid double-counting the fee.
- (decision) AD-14 NEW (adversarial HIGH): src/lib/enrollment.ts is the SOLE writer of every Enrollment.status transition (create, confirm, cancel, expire, ATTENDED, NO_SHOW). Admin attendance (Story 6.2) and lazy expiry call enrollment.ts functions; the webhook calls enrollment.ts to confirm. Resolves the AD-4/AD-7 'sole confirmer' wording: AD-4 owns the transition FUNCTIONS; callers never UPDATE status directly. Every status write goes through the module that can take the right lock.
- (decision) AD-15 NEW (data-integrity HIGH + adversarial HIGH): late/orphan payment money conservation. If a charge.success webhook arrives after the hold expired and the seat is gone, the captured money is CREDITED TO WALLET BALANCE (CANCELLATION_REFUND-style ledger row via AD-6), not silently kept and not oversold. MVP conserves money as wallet credit; Paystack card-refund adapter is deferred. Idempotent by Payment.reference. This overrides epics Story 4.2 wording ('auto-refunded via Paystack') -> flag to update epics.
- (decision) AD-16 NEW (adversarial HIGH): admin GroupSession edits are governed. capacity/price/meetingUrl edits run under the same GroupSession FOR UPDATE lock; the capacity-cannot-drop-below-occupied check is evaluated INSIDE that lock (else it races reserveSeat into oversell). Add GroupSession.updatedAt @updatedAt and an optimistic-concurrency check (reject stale writes) so two admins can't silently clobber each other. Enrollment.priceCents is the immutable snapshot at reserve-time; later class price edits never change existing enrollments' charge.
- (decision) Schema deltas required (spine drives migrations beyond the init migration): (1) GroupSession.updatedAt @updatedAt (AD-16); (2) partial unique index on LedgerEntry(enrollmentId) WHERE type='BOOKING_CHARGE' via raw SQL (AD-8); (3) consider FK + relation on LedgerEntry.studentId -> User (currently a bare string) for integrity; (4) optional unique on Enrollment.paymentRef. @prisma/adapter-pg ^6.7.0 predates driver-adapter GA (6.16.0): either add generator previewFeatures=["driverAdapters"] or bump to >=6.16.
- (decision) Operational envelope (rubric LOW): rate-limit the magic-link send endpoint (email-bomb) and the webhook; add error monitoring/alerting on the money paths (reserve, webhook, cancel); DB backups required for NFR5 auditability; keep Paystack test vs live keys env-split with a guard; webhook route runtime='nodejs'. Captured as conventions + open ops items.
- (version) Version currency (version reviewer): next 16.1.1 -> bump to current 16.2.x (latest 16.2.10; 16.1 line at 16.1.7) and match eslint-config-next; react/react-dom 19.2.3 -> 19.2.7 (patch); better-auth floor ^1.2.8 -> pin exact current 1.6.x (latest 1.6.22/1.6.23). Exact-pin the security-sensitive ones.
- (event) All gate fixes logged: AD-1/3/4/5/6/7/8/11 revised, AD-14/15/16 added, schema deltas + ops + versions captured. Re-distilling spine.
- (event) spine finalized — 16 ADs (13 original + AD-14/15/16 from gate), lint clean, status:final. Gate found + closed: per-student balance lock, sole status writer, enforced single BOOKING_CHARGE, late-webhook money conservation, admin-edit concurrency, layout-is-not-a-guard, SSI oversell guarantee. AD-15 supersedes epics Story 4.2 wording.
