---
topic: ACCE SEO page architecture — hub-and-spoke of dedicated indexable subject/qualification pages + guide release to fix single-URL ranking
updated: 2026-07-11T12:42
---

- (event) Create from SEO page-architecture brief (2026-07-11_page-architecture-brief.md), rich pre-structured input → express distill, no elicitation
- (direction) Luke: SEO is priority; build on a branch off main (marketing-only, independent of the never-run-live epic-6 portal)
- (note) Problem: every subject/qualification lives on one URL (homepage anchors); Google has one page to rank for Accounting/Tax/Audit/MAF/CTA/PGDA at once; homepage ~p20, only 2 URLs earn GSC impressions
- (decision) Fix = hub-and-spoke of dedicated indexable pages: 4 subject spokes + 2 qualification hubs + /subjects hub, cross-linked; homepage sections kept (they convert)
- (capability) CAP-1 /subjects hub: intent=one indexable page introducing the 4 subjects+2 qualifications and routing to each; success=live with unique title/H1, BreadcrumbList schema, links to all 4 spokes + both hubs
- (capability) CAP-2 /accounting-tutor spoke (Phase 1, biggest volume): intent=dedicated page ranking 'accounting tutor' (p34) + variants; success=live, unique title/H1, 700-900w differentiated, Service+FAQPage schema, cross-links; GSC shows it as a distinct ranking URL
- (capability) CAP-3 /financial-management-tutor spoke (MAF): intent=rank 'financial management tutor' (p27) + management-accounting/finance/MAF variants; success=live, unique title/H1, 700-900w, Service+FAQPage schema, cross-links
- (capability) CAP-4 /tax-tutor spoke: intent=rank 'tax tutor' + taxation variants; success=live, unique title/H1, 700-900w, Service+FAQPage schema, cross-links
- (capability) CAP-5 /auditing-tutor spoke: intent=rank 'auditing/audit tutor' + ISA variants; success=live, unique title/H1, 700-900w, Service+FAQPage schema, cross-links
- (capability) CAP-6 /cta-tutor qualification hub (Phase 1, FLAGSHIP — already p3): intent=defend & expand 'cta tutors' and route to the 4 subjects; success=live, unique title/H1, 700-900w, Course+FAQPage schema, links to all 4 spokes + /pgda-tutor; p3 held or improved
- (capability) CAP-7 /pgda-tutor qualification hub: intent=rank 'pgda tutor' + PGDA->CTA->CA(SA) pathway, route to 4 subjects; success=live, unique title/H1, 700-900w, Course+FAQPage schema, links to 4 spokes + /cta-tutor
- (capability) CAP-8 Guide release (Phase 1): intent=un-gate the already-built finished guides (/guides/groups, /guides/ifrs-15, /guides/ifrs-16) so real expert content is indexable (kills 'thin site', feeds subject-page internal links); success=guides publicly reachable + PDFs, listed in sitemap, indexable
- (capability) CAP-9 Navigation + internal-link matrix: intent=navbar routes to the new pages (Subjects->/subjects + Qualifications CTA/PGDA), homepage Services cards get 'Learn more' links to spokes, hub<->spoke cross-links wired; success=every new page has 2-4 internal links in/out; nav reaches all new pages
- (capability) CAP-10 Per-page SEO metadata + JSON-LD + sitemap: intent=each page exports title/description/canonical/OG/Twitter (mirror homepage pattern) + injects JSON-LD, and all new routes added to src/app/sitemap.ts; success=every new URL has unique metadata, valid schema, and appears in sitemap
- (capability) CAP-11 Measurement via GSC pipeline: intent=on publish submit each URL for indexing, then track each page's primary query monthly through the ClaudeSEO GSC snapshot; success=baselines (accounting tutor 34 / financial management tutor 27 / cta tutors 3) tracked and moving, new URLs appear in GSC Top Pages
- (constraint) ANTI-DOORWAY (load-bearing): every page 700-900 words of genuinely differentiated content (real syllabus detail + exam technique); templated near-duplicate pages are forbidden (Google scaled-content-abuse risk would undercut the whole effort)
- (constraint) Per-page SEO floor: exactly one <h1>; title <= ~60 chars; meta description <= ~155 chars; JSON-LD per page (Service / Course / FAQPage / BreadcrumbList as specified per page)
- (constraint) Drop ACCA: ignore acca queries (noise, not our market); target the CA(SA)/CTA/PGDA pipeline only (Accounting, MAF, Tax, Auditing, CTA, PGDA + undergrad)
- (constraint) Keep homepage sections intact — new pages RANK, homepage sections CONVERT; do not remove/restructure homepage sections when spokes go live
- (constraint) Additive only: existing flat marketing routes and their SEO/security headers stay unchanged; no regressions to current pages
- (constraint) Mandatory hub<->spoke cross-linking: every new page carries 2-4 internal links in/out per the link matrix — this is what routes ranking authority to the spokes
- (constraint) Reuse existing navy+gold design system (globals.css tokens, shadcn primitives, Navbar/Footer, Button hero variant); no new palette or components; follow the existing guide-page structure (Navbar + content + Footer)
- (constraint) Weave SA E-E-A-T context for depth: CA(SA), SAICA, ITC/APC board exams, universities (UNISA/UCT/Wits/UJ/UP/Stellenbosch)
- (constraint) Guide-release mechanics: flip src/config/guides.ts GUIDE_PUBLISH_STATUS to true AND mirror the same flip in src/middleware.ts (Edge runtime can't import the config); sitemap already gates guide URLs on publish status
- (decision) Phasing (sequencing for epics, not a hard spec gate): Phase 1 = CAP-6 /cta-tutor + CAP-2 /accounting-tutor + CAP-8 guide release + nav/sitemap for those; Phase 2 = remaining spokes + /pgda-tutor + /subjects + full link matrix + homepage Learn-more; Phase 3 (non-goal here) = promote About/HowItWorks/Pricing/Contact to pages, geo/university long-tails, testimonials
- (note) Naming nuance: site subject is 'Management Accounting' but GSC demand is 'financial management tutor'; in CTA this is one subject (MAF). /financial-management-tutor targets both
- (assumption) The 3 guides are content-complete and accurate enough to publish (brief §7 asserts 'already built'); flip-to-live assumes Priyanka/Luke signs off on accuracy — flagged for confirmation before release
- (note) Non-goals: Phase 3 page promotions (About/HowItWorks/Pricing/Contact), geo/university long-tail pages, added testimonials; the portal/auth/payments track; ACCA content; any new palette/design system; removing homepage sections
- (note) Success signal: target queries move toward page 1 (defend cta tutors <=p3; accounting tutor 34->page1; financial management tutor 27->page1), tracked monthly via ClaudeSEO GSC snapshot; GSC Top Pages grows from 2 URLs to the new set each earning impressions; every new URL submitted for indexing on publish
- (note) Companion page-catalog.md holds the per-page matrix (URL, primary/secondary kw, title, meta, H1, H2s, schema type, links in/out) + the hub<->spoke link matrix + phasing — multi-item catalog, too big for the kernel
- (note) Adopted companion: DESIGN.md (navy+gold visual system) — page builders must match it. Source fully absorbed: the SEO page-architecture brief
- (event) Self-validate Pass 1 (Coherence, Spec Law 1-6,8): PASS — 11 capabilities each carry intent+success; constraints all bend design; 5 non-goals; success signal is GSC-testable; IDs CAP-1..11 unique
- (event) Self-validate Pass 2 (Preservation): PASS — every load-bearing brief claim landed (problem->Why; kw/ACCA->CAPs+constraints; URL arch+page specs->page-catalog; nav/link matrix->CAP-9; guides->CAP-8; measurement->CAP-11; phasing->page-catalog; anti-pattern guard->anti-doorway constraint). No load-bearing drops
- (event) Spec finalized: SPEC.md + page-catalog.md companion; DESIGN.md adopted; brief listed as source. 2 open questions raised (guide accuracy sign-off; navbar Subjects timing)
- (direction) Luke: cut branch feat/seo-page-architecture off main for the SEO work (done)
- (decision) RESOLVES open-question #1: guides are NOT released yet — Luke re-reviews content for accuracy first; CAP-8 stays gated pending his sign-off (supersedes the earlier 'content-complete enough to publish' assumption)
- (decision) SCOPE SPLIT for guide release: when a guide goes live, release the WEB PAGE ONLY. PDFs stay gated — they are undergoing full content + structural review. Release page and PDF independently
- (constraint) Guide page/PDF gating must be DECOUPLED: today one GUIDE_PUBLISH_STATUS flag drives both the /guides/<id> page and its /pdfs/<file>.pdf (in src/config/guides.ts AND src/middleware.ts). Add an independent PDF-publish state so a page can be public while its PDF stays blocked; keep both files in sync (Edge runtime can't import the config)
- (constraint) Guide-page 'Download PDF' CTA must be CONDITIONAL on the PDF-publish state — hide/disable it while the PDF is held, else the button links to a middleware-redirected (gated) PDF and dead-ends at /guides
- (event) Re-derive SPEC.md + page-catalog.md after page/PDF decoupling + review-gating decisions
- (decision) RELEASE ALL AT ONCE: no phased rollout. All 7 pages + nav/link matrix + guide-page release ship in one release. The brief's 'Phase 1 (cta+accounting) / Phase 2 (rest)' was suggested sequencing only — collapsed. Build order may still start with the flagship pages, but nothing ships partially; the full hub<->spoke link matrix must be complete at release (no links pointing at unbuilt pages)
- (decision) RESOLVES open-question #2: since all pages ship together, /subjects exists at release — navbar repoints Subjects -> /subjects and adds Qualifications (CTA/PGDA) as the brief intended; no interim 'CTA Tutoring only' nav state needed
- (event) Re-derive complete: CAP-8 = web pages only (PDFs held); added page/PDF decoupling + conditional-download-CTA + single-release constraints; both open questions resolved -> none remaining. Self-validate Pass 1 (coherence) + Pass 2 (preservation) re-run: PASS
- (direction) Luke: the page/PDF gating decoupling is NOT built now — schedule it as an explicit story in the bmad-create-epics-and-stories pass (prereq for CAP-8 release)
- (constraint) EDITORIAL (load-bearing): NO em dashes in any page copy. Luke: em dashes read as AI-generated and undercut the human expert E-E-A-T voice. Use commas, colons, parentheses, or split sentences. Applies to headings, body, FAQs, and meta descriptions on every page
- (decision) SCOPE ADD: em-dash cleanup covers the WHOLE SITE in this release, not just new pages. All existing rendered marketing copy + metadata (homepage components, guide pages + part pages, page metadata) gets em dashes removed too
- (capability) CAP-12 Site-wide em-dash cleanup: intent=existing site copy is scrubbed of em dashes so the whole site reads human (not AI); success=zero em-dash chars across rendered marketing copy + metadata (homepage components, guide pages/parts, all page metadata), meaning/tone preserved. Scope = em dashes only; leave en dashes in numeric ranges like 24-48h
- (note) Cleanup is a content/copy edit only; does not change routes, SEO structure, or headers, so it does not violate the additive-only constraint
- (event) Re-derive: add CAP-12 site-wide em-dash cleanup; broaden editorial constraint from page copy to site-wide
- (event) Self-validate re-run after CAP-12 add: Pass 1 coherence + Pass 2 preservation PASS; 12 capabilities, editorial constraint now site-wide, 0 open questions
